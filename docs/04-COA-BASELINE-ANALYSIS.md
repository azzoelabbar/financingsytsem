# تحليل الـBaseline — الاكتشافات (Findings 1–5)

تحليل كامل لـ168 حساباً في `نموذج دليل الحسابات.xlsx`. كل قرار مُطبَّق في المولّد ومُتحقَّق منه.

## 1) الحسابات المصنَّفة بطريقة غير صحيحة (Misclassified)

| الرمز | الاسم في الملف | المشكلة | التصحيح |
|---|---|---|---|
| `110204` | مخصص الديون المشكوك فيها | مُدرج كـ«أصل» عادي | **Contra-Asset / مخصص ECL** — `is_contra`, `contra_of=110201`, `ifrs=IFRS 9`. |
| `120103/05/07/09/11` | «م.م. …» | اسم مختصر، تصنيف أصل | **Accumulated Depreciation (Contra)** — إعادة تسمية كاملة + `contra_of` للأصل الأصلي. |
| `410103` | مردودات المبيعات | «إيراد/مدين» بلا ربط | **Contra-Revenue** — `contra_of=4101`. |
| `510103` | مردودات المشتريات | «مصروف/دائن» بلا ربط | **Contra-Expense** — `contra_of=5101`. |
| `3203/320301/320302` | المسحوبات/التوزيعات | «حقوق ملكية» مع علم «ختامي=نعم» | **Contra-Equity** دائمة — فُصل `closing_behavior` عن `statement`؛ لا تُعامَل كحساب مؤقت. |
| `320103` | احتياطي إعادة التقييم | حقوق ملكية عادية | يُصنَّف **OCI** (احتياطي دخل شامل آخر) — `IAS 16`. |
| `420201` | «إيجارات مستحقة» | اسم مضلِّل (يوحي باستحقاق) | أُعيدت تسميته **«إيرادات إيجار» / Rental Income** — فهو إيراد تشغيلي. |
| `7101/7102` | الأرباح والخسائر/المتاجرة | حسابات ختامية | تُثبَّت **system-only** (`manual_journal_allowed=false`) وخارج القوائم. |

## 2) الحسابات المكررة (Duplicates)

- **لا تكرار في الأرقام:** الـ168 رمزاً كلها فريدة (تم التحقق).
- **تداخلات مفاهيمية (ليست تكراراً، وُثّقت لتفادي الخلط):**
  - `620103 مكافأة نهاية الخدمة` (مصروف P&L) مقابل `220201 مخصص مكافآت نهاية الخدمة` (مخصص ميزانية) — صحيح: طرفا IAS 19 المختلفان، لا تكرار.
  - `320103 احتياطي إعادة التقييم` — أُبقي كما هو ومُنع تكراره في مجموعة `33` الجديدة؛ الاحتياطيات الأخرى للـOCI أُضيفت منفصلة.
  - `630103 فروق العملة` (مصروف مجمّع) — لا يُحذف بل يُكمَّل بفصل محقق/غير محقق (انظر البند 6 من التحليل السابق).

## 3) الحسابات الناقصة (Missing) — أُضيفت (125 حساباً)

مجمّعة حسب الغرض (التفاصيل الكاملة في `coa_enterprise_libya.json`):

- **IFRS 15:** أصل عقد `110207`، التزام عقد `210302`، إيراد اشتراكات مؤجل `210303`، دفعات عملاء `210304`، إيراد على مدى الزمن `410401`.
- **IFRS 16 (إيجارات):** حق استخدام `120112/113` + مجمع استهلاكه `120114`، التزام إيجار متداول `210503` وغير متداول `220301`، استهلاك حق الاستخدام `620407`، فائدة الالتزام `630106`.
- **IFRS 9 (أدوات مالية):** FVTPL `110603`، FVOCI `120304`، تكلفة مطفأة `120305`، مشتقات أصول `110604`/التزامات `210901`، مصروف ECL `630203`، احتياطي FVOCI `330101`.
- **IAS 12 (ضريبة مؤجلة):** أصل `120401`، التزام `220501`، مصروف ضريبة جاري `640101`/مؤجل `640102`.
- **IAS 36 (انخفاض القيمة):** مجمع انخفاض PPE `120115`/الشهرة `120206`، خسائر انخفاض `620501/502/503`.
- **IAS 38 (غير ملموسة):** مجمع إطفاء `120205`، تكاليف تطوير مرسملة `120207`.
- **IAS 19:** التزام منافع محددة `220401`، إعادة قياس (OCI) `330104`.
- **IAS 21 (عملات):** أرباح/خسائر محققة `420104/630104`، غير محققة `630105`، احتياطي ترجمة `330102`.
- **العملات/الضرائب/الرواتب:** Input/Output VAT `110210/210404`، VAT مستحق `210405`، دمغة `210406`، تضامن `210407`، PAYE `210702`، ضمان اجتماعي موظف/صاحب عمل `210703/704`، صافي رواتب `210701`.
- **التوحيد (Consolidation):** ذمم/دائن بين الشركات `110209/210108/1205/2206`، حقوق غير مسيطرة NCI `310104`.
- **OCI / حقوق ملكية:** مجموعة `33` كاملة، أسهم خزينة `310103`، تعديلات فترات سابقة `320203` (IAS 8).
- **Suspense/Clearing:** مجموعتا `1107`/`2108` (مقبوضات، تحويلات، رواتب، POS، معلّق مدين/دائن).
- **Memo / Off-BS (فئة 8):** ضمانات صادرة/واردة، التزامات محتملة، التزامات رأسمالية، بضاعة أمانة.
- **CWIP / استثمار عقاري / أصول بيولوجية:** `120116/120117/120119` (IAS 16/40/41).

## 4) الحسابات التي تحتاج Contra Account — عولجت

| الأصل/الحساب | الـContra المرتبط |
|---|---|
| الأصول الثابتة (مبانٍ/آلات/أثاث/سيارات/أجهزة) | مجمعات الاستهلاك `1201xx` (موجودة، صُحّحت) |
| حق استخدام الأصل | مجمع استهلاك حق الاستخدام `120114` (مضاف) |
| الأصول غير الملموسة | **مجمع إطفاء `120205`** (كان ناقصاً — مضاف) |
| الشهرة / PPE | مجمعات انخفاض القيمة `120206/120115` (مضاف) |
| العملاء / الذمم | مخصص ECL `110204` (رُبِط) |
| المخزون | مخصص هبوط/تقادم عبر `620503` |
| المبيعات / المشتريات | مردودات `410103/510103` (رُبطت) |
| رأس المال | أسهم الخزينة `310103` + المسحوبات/التوزيعات (Contra-Equity) |

## 5) الحسابات التي تحتاج Control Account — حُدِّدت

| الحساب | الأستاذ المساعد (Subledger) | تسوية |
|---|---|---|
| `110201` العملاء | AR (عملاء) | نعم |
| `210101` الموردون | AP (موردون) | نعم |
| `110301` المخزون | INV (أصناف/مستودعات) | نعم |
| `1201` الأصول الثابتة | FA (سجل الأصول) | — |
| `110102/110103` البنوك | BANK (كشوف بنكية) | نعم (تسوية بنكية) |
| `2104x` الضرائب | TAX (بنود ضريبية) | نعم |
| `2107x` الرواتب | PAYROLL (كشوف الرواتب) | نعم |
| `110601` سلف الموظفين | PAYROLL | نعم |

> ملاحظة: التفاصيل التشغيلية (أرصدة كل عميل/مورد/أصل) تعيش في **الأستاذ المساعد** لا في GL؛ حساب المراقبة يحمل الإجمالي فقط، وتتم مطابقته دورياً (GL vs Subledger — بند 43).
